# Project-Scoped AGENTS Rules

## GitHub Automatic Sync Directive
- After creating or editing files in any phase or task, automatically run `git add .`, create a descriptive commit, and push changes to the remote GitHub repository (`git push origin main`).
